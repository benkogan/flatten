{
  "name": "flatten",
  "version": "0.1.0",
  "devDependencies": {
    "should": "^4.0.4",
    "rimraf": "^2.2.8"
  },
  "scripts": {
    "test": "mocha --reporter spec --check-leaks test/"
  },
  "dependencies": {
    "commander": "^2.3.0",
    "cp": "^0.1.1"
  }
}
